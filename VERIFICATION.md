# 验证记录

日期：2026-09-24

## 自动检查

- `npm test`：28/28 通过。
- `node --check extension/index.js`：通过。
- `install.ps1`、隔离运行时启动/停止脚本：PowerShell 解析通过。
- 未在工作包中发现用户提供的真实 Token；测试使用三段伪 JWT。

覆盖范围：来源白名单、HTTPS 与 JWT 结构、请求体大小、模型/尺寸/采样器/步数/CFG 范围、双鉴权头、固定上游路径、上游错误、不重试、非 SSE、SSE 分块/截断/final、背压、超时、客户端取消、Token 脱敏、图片上传、聊天保存读回、角色/群组契约、聊天切换竞态和保存失败。

## 隔离酒馆运行验证

目标：用户本地 `M:\SillyTavern`，版本 1.13.3；验证副本：`verification/tavern-runtime`；地址：`http://127.0.0.1:8917`。

- 副本使用空数据目录和独立插件目录，原安装、原聊天、原 Token 未修改。
- 服务端插件健康检查：`GET /api/plugins/paintai-bridge/health` 返回 `{"ok":true,"version":"1.0.0"}`。
- 浏览器面板：URL、Token、提示词、参数、额度检查和生成按钮可见。
- 模拟额度检查：显示“剩余 100 点”，且没有真实扣费。
- 模拟 SSE：中间图和最终 PNG 均显示；最终图可下载。
- 加入角色聊天：PaintAI 消息和图片出现；刷新页面重新打开聊天后图片仍存在。
- 错误事件：`fixture-error` 显示“模拟上游失败”，未插入聊天。
- 取消：`fixture-slow` 可取消并恢复按钮；随后再次生成成功。
- 窄屏：390×844 面板单列排列，输入框、按钮、状态和图片没有明显遮挡或横向溢出。

隔离服务已停止。由于本轮没有使用真实网站出图，真实 NovelAI 网络、账号池余额、实际扣费和生产反向代理配置仍需用户安装后自行验收。
