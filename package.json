{
  "name": "sillytavern-paintai-bundle",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test tests/*.test.mjs server-plugin/tests/*.test.mjs" },
  "engines": { "node": ">=20" }
}
